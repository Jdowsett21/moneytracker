import React from 'react';

const FormSuccess = ({ text }) => (
  <section className=''>
    <p className=''>
      <span className=''>{text}</span>
    </p>
  </section>
);

export default FormSuccess;
